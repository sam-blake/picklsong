$('.search-results').html("<%= j(render("results/vidresults"))%>");

$(document).ready(function() {
    $('.vid-item').click(function() {
        console.log('clicked');
        var vidAttributes = $(this).data('attributes');
        var activePlaylist = $('.active-playlist').data('id');
    var createSong = function(){
    var song_id;
        $.ajax({
            type: "POST",
            url: "/songs",
            data: {
                song: vidAttributes,
                playlist: activePlaylist
            },
            dataType: 'script',
            success: function(resp){
                console.log(get_song_id());
                song_id = get_song_id();
                console.log("song is " + song_id);
                }
            });
      console.log(song_id);
    };
        vidAttributes.id = createSong();
        console.log(vidAttributes);
        var playlist = $('#' + activePlaylist);
        var addMe = playlist.data('songs');
        addMe.push(vidAttributes);
        playlist.attr('data-songs', JSON.stringify(addMe));
    });
});

$(document).ready(function() {
    $(".arrow-right").bind("click", function(event) {
        event.preventDefault();
        $(".vid-list-container").stop().animate({
            scrollLeft: "+=336"
        }, 750);
    });
    $(".arrow-left").bind("click", function(event) {
        event.preventDefault();
        $(".vid-list-container").stop().animate({
            scrollLeft: "-=336"
        }, 750);
    });
});